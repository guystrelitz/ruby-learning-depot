<% puts "**************** rendering partial ****************" %>
<tr>
	<td class="quantity"><%= line_item.quantity %></td>
	<td><%= line_item.product.title %></td>
	<td class="price"><%= number_to_currency line_item.total_price %></td>
	<td class="buttons"><%= button_to '+', line_items_path(product_id: line_item.product) %></td>
	<td class="buttons"><%= button_to '-', line_item_path(line_item.id), method: :delete, params: {remove_one: true} %></td>
</tr>